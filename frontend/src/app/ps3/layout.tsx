import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'RailGuard AI — Fleet intelligence',
  description: 'Inspect recorded railway data, subsystem predictions, supporting evidence and measured local validation.',
};
export default function Ps3Layout({ children }: { children: React.ReactNode }) { return children; }
