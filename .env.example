# Copy to .env locally. Never commit a real credential or put it in frontend code.
# Predictions, validation and exports require no external AI service.
RAILGUARD_AI_ENABLED=true
RAILGUARD_AI_MODEL=gpt-5.4-mini
OPENAI_API_KEY=
